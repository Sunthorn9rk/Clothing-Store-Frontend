import {createTheme} from "@mui/material/styles";

// กำหนดธีมให้ใช้ฟอนต์ Agu Display
const theme = createTheme({
  typography: {
    fontFamily: '"Agu Display", "Roboto", "Helvetica", "Arial", sans-serif',
  },
});

export default theme;
